# Phase 15 macro placement.
#
# <instance> <x_um> <y_um> <orientation>
#
# One instance of the 2 KiB OpenRAM SRAM (683.1 x 416.54 um), placed in the
# lower-left of the core so the standard-cell rows keep a contiguous region.
soc.g_sram.ram.macro 200 200 N
