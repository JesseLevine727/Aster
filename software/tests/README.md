# Firmware tests

Directed bare-metal images live here. They are intentionally small and report
their result through the simulation UART so the same SoC harness can validate
CPU, memory and peripheral behavior.
